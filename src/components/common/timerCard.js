import React from "react";

// I don't think this is even necessary but maybe?
const TimerDisplay = ({ time }) => {
    return (
            <h2>Timer: {time}</h2>
    );
};

export default TimerDisplay;